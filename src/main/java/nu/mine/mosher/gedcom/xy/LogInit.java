package nu.mine.mosher.gedcom.xy;

//import ch.qos.logback.classic.LoggerContext;
//import ch.qos.logback.core.status.*;
//import org.slf4j.*;
//
//import java.util.Objects;

public class LogInit {
    public static void init() {
//        final ILoggerFactory factory = LoggerFactory.getILoggerFactory();
//        if (factory instanceof LoggerContext ctx) {
//            ctx.getLoggerList().forEach(l -> System.out.println(l.getName()));
//            System.out.flush();
//
//            final StatusManager statusManager = ctx.getStatusManager();
//            if (Objects.nonNull(statusManager)) {
//                statusManager.add(new InfoStatus("Configuring logger", ctx));
//            }
//        } else {
//            throw new IllegalStateException("Logging implementation must be Logback.");
//        }
    }
}
